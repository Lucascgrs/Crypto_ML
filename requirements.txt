# --- Données ---------------------------------------------------------------
pandas            # >= 1.4 pour rolling(...).rank(), le rang glissant du panier
numpy
requests          # Binance spot + futures (funding, basis, open interest), CoinGecko
yfinance          # source de repli quand une crypto n'est pas sur Binance
openpyxl          # lecture / écriture des fichiers Excel
pyarrow           # cache Parquet : recharge les analyses ~50x plus vite

# --- Modèles ---------------------------------------------------------------
scikit-learn      # >= 1.0 pour HistGradientBoostingRegressor(loss="quantile")
xgboost           # >= 2.0 pour l'objectif reg:quantileerror
lightgbm
catboost
joblib

# --- Interface et graphiques ----------------------------------------------
customtkinter
matplotlib

# --- Système ---------------------------------------------------------------
psutil            # mesure RAM / CPU pour dimensionner l'entraînement
